{
  "name": "hanzi-site",
  "version": "0.1.0",
  "private": true,
  "description": "HanziWriter 静态教学站点 + 一键同步到 Nginx 目录脚本",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.js && node scripts/deploy-nginx.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1"
  }
}

